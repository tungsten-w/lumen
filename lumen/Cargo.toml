[package]
name = "lumen"
version = "2.3"
edition = "2024"

[dependencies]
chrono = "0.4"
rand = "0.8"
serde_json = { version = "1", features = ["preserve_order"] }

[profile.release]
lto = "fat"
codegen-units = 1
strip = true
